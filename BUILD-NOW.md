# 快速构建指南

## 当前项目状态

✅ **所有资源问题已修复**
- XML 资源文件已添加
- 应用图标已创建
- AppCompat 依赖已添加
- Gradle Wrapper 已配置

## 🚀 立即开始构建

### **方式 1：使用 build.bat（最简单）**

```cmd
# 直接双击 build.bat
# 或在命令行执行
build.bat
```

### **方式 2：使用 Gradle Wrapper**

```cmd
# Windows
gradlew.bat build

# Linux/macOS
./gradlew build
```

### **方式 3：使用 Android Studio**

1. 打开 Android Studio
2. File → Open → 选择项目目录
3. 等待 Gradle 同步完成（自动下载依赖）
4. 点击 Run 按钮 (或按 Shift+F10)

## 📦 构建产物位置

成功构建后，APK 文件位于：
```
app/build/outputs/apk/debug/app-debug.apk
```

## 🔧 如果构建失败

### 清理并重新构建

```cmd
gradlew.bat clean
gradlew.bat build
```

### 删除缓存

```cmd
rd /s /q .gradle
rd /s /q app\build
gradlew.bat build
```

### 查看详细日志

```cmd
gradlew.bat build --info --stacktrace
```

## 📱 安装到设备

### 使用 adb

```cmd
# 安装 APK
adb install app/build/outputs/apk/debug/app-debug.apk

# 启动应用
adb shell am start -n com.yinian.clipboard/.ui.MainActivity

# 查看日志
adb logcat | grep "yinian"
```

### 直接在手机上安装

1. 将 `app-debug.apk` 复制到手机
2. 在手机文件管理器中点击 APK 文件
3. 允许安装来自未知来源的应用
4. 点击安装

## ✅ 构建成功检查清单

- [ ] 无编译错误
- [ ] `app-debug.apk` 文件已生成
- [ ] APK 大小约 5-10 MB
- [ ] 可以安装到 Android 设备
- [ ] 应用启动无崩溃

## 🎯 下一步

1. **运行测试**：执行 `test.bat`
2. **手动测试**：参考 [docs/TESTING-GUIDE.md](docs/TESTING-GUIDE.md)
3. **开始开发**：修改代码并重新构建

## 💡 提示

- 首次构建需要下载依赖，可能需要 5-10 分钟
- 确保网络连接正常（需要访问 Google Maven）
- JAVA_HOME 已自动配置为 Android Studio 自带的 JDK
- 构建过程中可以看到下载进度条

## 🐛 常见问题

**Q: 构建失败提示网络错误**
A: 检查网络连接，或使用国内镜像源（在 `build.gradle.kts` 中配置）

**Q: 提示找不到 Android SDK**
A: 检查 `local.properties` 文件中的 `sdk.dir` 路径是否正确

**Q: 构建成功但安装失败**
A: 确保手机开启了"USB 调试"和"允许安装未知来源应用"

---

**现在可以开始构建了！** 🎉

运行 `build.bat` 或直接在 Android Studio 中打开项目。
